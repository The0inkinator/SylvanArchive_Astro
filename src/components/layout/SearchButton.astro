---
---

<button>Search</button>

<style>
  button {
    all: unset;
    height: calc(var(--HeaderHeight) * 0.6);
    width: calc(var(--HeaderHeight) * 0.6);
    background-color: white;
    border-radius: 50%;
    border: solid black;
    text-align: center;
    align-self: center;
    margin-right: calc(var(--ButtonLeafHeight) * 0.33);
  }
</style>
