---
---

<script >
const searchBar = document.getElementById("searchBar") as HTMLDivElement;
const logo = document.getElementById("searchBarLogo") as HTMLDivElement;
const input = document.getElementById("searchBarInput") as HTMLInputElement;
const title = document.getElementById("searchBarTitle") as HTMLDivElement;
const x = document.getElementById("x") as HTMLDivElement;

let barScrolled = false;
let barClicked = false;


// window.addEventListener("scroll", () => {
//   if (window.scrollY > 0) {
//     searchBar.style.width = "calc(var(--SearchBarHeight) * 1.3)"
//     input.style.display = "none"
//     title.style.transition = "width 0.25s"
//     title.style.width = "0"
//     x.style.display = "none"
//     barScrolled = true;
//     barClicked = false;
//     } else {
//     searchBar.style.width = "calc(var(--SearchBarHeight) * 4.4)"
//     input.style.display = "none"
//     title.style.transition = "width 0.25s"
//     title.style.width = "calc(var(--SearchBarHeight) * 2.8)"
//     x.style.display = "none"
//   }

// })

// searchBar.addEventListener("click", () => {
//   if (barClicked === false) {
//     searchBar.style.width = "calc(var(--SearchBarHeight) * 2.2 + var(--SearchBarWidth))"
//     input.style.display = "block"
//     title.style.transition = "width 0s"
//     title.style.width = "0"
//     x.style.display = "grid"
//     barClicked = true;
//   } else {x.addEventListener("click", () => {if (barClicked === true) {
//     console.log("x clicked")
//     searchBar.style.width = "calc(var(--SearchBarHeight) * 4.4)"
//     input.style.display = "none"
//     title.style.transition = "width 0s"
//     title.style.width = "calc(var(--SearchBarHeight) * 2.8)"
//     x.style.display = "none"
//     console.log(barClicked)
//     }
//     }
   
//     )}

})

</script>

  <div class="searchBarContainer">
  <div id="searchBar">
  <div id="searchBarLogo"></div>
  <input id="searchBarInput" value="Start Searching" type="text">
   <div id="x">
  <div></div>
  <div></div>
  </div>
  <div id="searchBarTitle">Sylvan Archive</div>
  </div>
  </div>
 <style>

.searchBarContainer {
  pointer-events: auto;
}

#searchBar {
    background-color: white;
    height: var(--SearchBarHeight);
    border-radius: calc(var(--SearchBarHeight) / 5);
    display: flex;
    width: calc(calc(var(--SearchBarHeight) * 4.4));
    align-items: center;
    cursor: pointer;
    filter: drop-shadow(0px 0px 5px black);
    transition: width 0.25s;
  }

  #searchBarLogo {
  height: 80%;
  aspect-ratio: 1/1;
  background-image: url("../../logos/sylvanArchiveLogo.svg");
  margin-inline: calc(var(--SearchBarHeight) / 4);
}

  input {
  all: unset;
  display: none;
  width: var(--SearchBarWidth);
  transform: translateX(calc(var(--SearchBarHeight) / -8))
}

#searchBarTitle {
  font-family: var(--HeadingFont);
  font-size: calc(var(--SearchBarHeight) * .5);
  white-space: nowrap;
  margin-right: calc(var(--SearchBarHeight) / 4);
  width: calc(var(--SearchBarHeight) * 2.8);
  overflow: clip;
  transition: width 0.25s;
}

#x {
  display: none;
  height: 80%;
  aspect-ratio: 1/1;
  place-items: center;
}



#x > * {
  width: 80%;
  height: 10%;
  background-color: lightgrey;
  grid-row: 1;
  grid-column: 1;
  align-items: center;
}

#x > :nth-child(1) {
  transform: rotate(45deg);
}
#x > :nth-child(2) {
  transform: rotate(-45deg);
}

  </style>
